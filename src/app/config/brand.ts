// The user-visible product name shown in the shared header branding (top bar and
// the mobile navigation drawer). This is DISPLAY branding only — internal
// identifiers, package/repo names, routes, APIs, and env vars keep the "HeyQ"
// codename and must not be renamed from here.
export const PRODUCT_NAME = 'QuadX Helpdesk';
